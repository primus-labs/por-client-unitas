services:
  por-client-unitas:
    # build: .
    image: primuslabs/por-client-unitas:v0.1.0
    container_name: por-client-unitas
    volumes:
      - ./.config.yml:/app/.config.yml:ro
      - ./.state.json:/app/.state.json
    restart: always
