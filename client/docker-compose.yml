services:
  por-client-unitas:
    # build: .
    image: primuslabs/por-client-unitas:v0.1.2
    container_name: por-client-unitas
    volumes:
      - ./.config.yml:/app/.config.yml:ro
    restart: always
